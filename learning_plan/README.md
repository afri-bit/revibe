# Your learning plan

After you run `/revibe-start`, the Mentor generates a **personal** plan file in this folder (for example `learning_plan/python-beginner.md`) with `status: active` in its frontmatter.

Everything under [`examples/`](examples/) is **reference material for contributors** only. The Mentor treats those files as inactive when coaching you.
