# =============================================================================
# Revibe is a markdown-only repo. Keep this file small and focused.
# Add entries only when something is actually likely to appear here.
# =============================================================================

# OS
.DS_Store
Thumbs.db
desktop.ini
*.swp
*.swo

# Editors
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json.example
.idea/
*.sublime-workspace
*.sublime-project

# Environment / secrets
.env
.env.*
!.env.example

# Logs and temp
*.log
*.tmp

# Local node tooling (only used by CI / docs lint; not required for users)
node_modules/
package-lock.json
.npmrc
